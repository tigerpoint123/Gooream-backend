apiVersion: v1
kind: Service
metadata:
  name: payment-svc-prod
spec:
  type: ClusterIP
  selector:
    app: payment-prod
  ports:
    - protocol: TCP
      name: http
      port: 8087       # 내부 서비스 포트
      targetPort: 8087 # 컨테이너 포트
    - name: metrics
      port: 8881
      targetPort: 8881