apiVersion: v1
kind: Service
metadata:
  name: gateway-svc-prod
spec:
  type: ClusterIP
  selector:
    app: gateway-prod
  ports:
    - protocol: TCP
      name: http
      port: 8000
      targetPort: 8000
    - name: metrics
      port: 8881
      targetPort: 8881