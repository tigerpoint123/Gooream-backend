apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-db-dpy-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-db-prod
  template:
    metadata:
      labels:
        app: product-db-prod
    spec:
      containers:
        - name: product-db-pod-prod
          image: mysql:8.2.0
          envFrom:
            - configMapRef:
                name: product-db-config-prod
            - secretRef:
                name: secret-common-prod
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: product-db-data
              mountPath: /var/lib/mysql
      volumes:
        - name: product-db-data
          persistentVolumeClaim:
            claimName: productdb-data-pvc