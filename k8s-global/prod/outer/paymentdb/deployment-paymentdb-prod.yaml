apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-db-dpy-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-db-prod
  template:
    metadata:
      labels:
        app: payment-db-prod
    spec:
      containers:
        - name: payment-db-pod-prod
          image: mysql:8.2.0
          envFrom:
            - configMapRef:
                name: payment-db-config-prod
            - secretRef:
                name: secret-common-prod
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: payment-db-data
              mountPath: /var/lib/mysql
      volumes:
        - name: payment-db-data
          persistentVolumeClaim:
            claimName: paymentdb-data-pvc