apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db-dpy-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db-dev
  template:
    metadata:
      labels:
        app: order-db-dev
    spec:
      containers:
        - name: order-db-pod-dev
          image: mysql:8.2.0
          envFrom:
            - configMapRef:
                name: order-db-config-dev
            - secretRef:
                name: secret-common-dev
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: order-db-data
              mountPath: /var/lib/mysql
      volumes:
        - name: order-db-data
          emptyDir: {}   # 개발용, 나중에 PVC로 변경 가능